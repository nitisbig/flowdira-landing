# Copy to .env. PUBLIC_* vars are inlined into the client bundle at build time.
# URL of the chat concierge Worker (see backend/). Local dev: wrangler dev.
PUBLIC_CHAT_API_URL="http://127.0.0.1:8787"
